docker --version

docker pull ubuntu

docker run -it ubuntu

docker images

docker run -d -p 8080:80 nginx

http://localhost:8080

docker ps

docker stop <container_id>
docker rm <container_id>
